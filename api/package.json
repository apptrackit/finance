{
  "name": "api",
  "scripts": {
    "setup-db": "bash setup-local-db.sh",
    "dev": "npm run setup-db && wrangler dev src/index.ts",
    "deploy": "wrangler deploy --minify src/index.ts",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "hono": "^4.0.0",
    "yahoo-finance2": "^3.10.2",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240208.0",
    "typescript": "^5.0.0",
    "vitest": "^4.1.4",
    "wrangler": "^4.83.0"
  }
}
